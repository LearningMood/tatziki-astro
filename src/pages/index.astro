---
import Layout from "../components/Layout.astro";
import FeaturedCarousel from "../components/FeaturedCarousel.astro";
import MasonryGridPlusFiller from "../components/MasonryGridPlusFiller.astro";
import { getCollection } from "astro:content";
import { getProjectImage } from "../utils/imageLoader";

const projects = await getCollection("projects", ({ data }) => {
  return data.draft !== true;
});

projects.sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
// Carousel | je sépare les projets à la une
const featuredProjects = projects.filter((p) => p.data.featured);
const regularProjects = projects.filter((p) => !p.data.featured);

// Trier les featured par ordre si défini
featuredProjects.sort((a, b) => {
  const orderA = a.data.featuredOrder || 999;
  const orderB = b.data.featuredOrder || 999;
  return orderA - orderB;
});

// Charger les thumbnails pour les projets featured
const featuredWithImages = await Promise.all(
  featuredProjects.map(async (project) => ({
    slug: project.slug,
    data: {
      ...project.data,
      heroImage: await getProjectImage(project, project.data.hero),
    },
  })),
);

// console.log('Nombre de projets trouvés:', projects.length);
// console.log('Liste des slugs:', projects.map(p => p.slug));

// ✅ NEW Utilise la fonction utilitaire
// Les seuls champs utilisés pour la home :
const projectsWithThumbnails = await Promise.all(
  projects.map(async (project) => ({
    slug: project.slug,
    data: {
      title: project.data.title,
      categories: project.data.categories,
      couleur: project.data.couleur,
      gridSize: project.data.gridSize, // ← Taille de la carte
      thumbnailImage: await getProjectImage(project, project.data.thumbnail), // ← Image
      thumbnail_alt: project.data.thumbnail_alt,
    },
  })),
);

// console.log("✅ Projets chargés:", projectsWithThumbnails.length);
---

<Layout title="Portfolio">
  {/* Carousel des projets à la une */}
  {
    featuredWithImages.length > 0 && (
      <FeaturedCarousel projects={featuredWithImages} />
    )
  }
  <header class="intro container">
    <h1 class="titre-intro">
      Tatziki
      <svg
        class="esperluette"
        xmlns="http://www.w3.org/2000/svg"
        version="1.1"
        viewBox="0 0 493.5 468.1"
      >
        <path
          d="M352.7,232.3c-.3.3-1,1.8-1.1,2.2-.3,2,4.8,8.9,6,11.3,28.6,54.2,16.5,114.5-24,158.4-83.6,90.5-240,85.9-308.4-20.3-33.7-52.3-33.8-122.6,1.8-173.9,4.3-6.2,12.4-13.5,15.6-19.4s1.8-4.2,1-6.4-7.5-9.9-9.7-13.2C-15.4,96.6,52.9,4.5,136,8.8c23.6,1.2,62.6,12.1,60.2,42.2s-19.8,29.2-36,24c-16-5.1-22.7-23.7-39.5-29-23.6-7.4-54.4,15.6-65.8,35-11.7,20-12.5,45.4-3.3,66.6,2.7,6.1,11.5,23.5,19.3,22.4,30.6-12.3,72.6-25.2,104.1-9.5,25.1,12.5,27.6,40.4,2.5,54.6-26.1,14.8-61.4,10.4-88.4.3-3.8-1.4-11.8-6.2-15.5-5.3s-9.2,11.7-10.9,15.5c-23.1,50.5-13.3,108.9,25.4,148.6,47.6,49,162.4,59.7,218.8,22,42.6-28.5,62.3-100.7,27-141.9-30.7-35.7-90.9-23.6-110.7,17.2-15.4,31.6,6.3,70.5,43.2,59.1,4-1.2,12.4-4.5,10.7-9.9-1-3.1-10.2-4.5-13.5-6-21.8-10-16.6-42.1,7-44.5,35.4-3.5,53.2,37.3,30.7,64.2-33.3,39.8-98.1,18.1-105.3-32-8.7-60.1,48.2-95.6,98.2-108.9,52.9-14.1,129.2-11.1,161.7-62.1s2.2-110.6-53.9-107.4c-2.6.1-14.8,1.3-13.7,5.7s6.7,8.6,8,11.5c15.4,33.1-37.1,53.8-50.1,17.6-9.6-26.9,13.1-51,37.9-57.1,43.5-10.6,87,23,101.9,62.2,30.3,79.8-32.6,157.7-112.5,165.5-4.4.4-18-.1-20.6,2.7l-.2.2ZM169,199c5.3-5.3,4.5-14.9-1.3-19.6-11.8-9.4-48-4.7-61.9,0s-15.7,5.7-12.9,11.4,17.9,9.7,22.6,10.8c12,2.9,44.4,6.5,53.5-2.6h0Z"
        ></path>
      </svg>
    </h1>Co
    <p>Les derniers projets en Design graphique & Développement web</p>
  </header>

<main class="container--md home-projects">
  <MasonryGridPlusFiller projects={projectsWithThumbnails} />
</main>
</Layout>
<!-- <MasonryGridPlusFiller projects={projects}/> -->
