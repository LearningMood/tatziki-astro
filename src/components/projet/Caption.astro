---

interface Props {
  content: string;
  author?: string;
  col: number;
  span: number;
  top: string;
  debug?: boolean;
  imageIndex?: number;
}

const { content, author, col, span, top, debug = false, imageIndex } = Astro.props;
---

<figure
  class="gallery-caption"
  style={`
    --col-start: ${col};
    --col-span: ${span};
    --margin-top: ${top};
  `}
>
  {debug && (
    <span class="debug-label debug-label--text">
      caption après img {imageIndex} | col {col} span {span}
    </span>
  )}
  <blockquote>{content}</blockquote>
  {author && <figcaption>{author}</figcaption>}
</figure>

<style>
  .debug-label--text {
    position: absolute;
    top: 0;
    left: 0;
    background: rgba(0, 150, 0, 0.9);
    color: white;
    padding: 4px 8px;
    font-size: 11px;
    font-family: monospace;
    z-index: 100;
  }
</style>